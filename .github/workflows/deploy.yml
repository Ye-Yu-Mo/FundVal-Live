name: Deploy

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          port: ${{ secrets.VPS_PORT || '22' }}
          username: ${{ secrets.VPS_USER || 'jas' }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd ~/fundval
            docker compose pull
            docker compose up -d --remove-orphans
            docker image prune -f
            docker compose ps
